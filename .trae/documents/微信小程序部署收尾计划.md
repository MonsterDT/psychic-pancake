# 微信小程序部署收尾计划（Phase 3-10）

## 任务背景

将 MakeupPal v3.6.0 HTML demo 完整转换为微信小程序原生代码，部署到微信开发者工具中运行。用户已确认：**完整转换全部 43 页**（5 主 tab 页 + 38 分包页），**AI 负责编译校验 + 代码合规检查**，**用户在微信开发者工具中手动测试**。

## 当前状态分析（Phase 1 探索结果）

### ✅ 已完成
- **Phase 0-2**：项目骨架完整
  - `app.json` / `app.js` / `app.wxss`（70+ CSS 变量挂在 `page` 选择器，合规）
  - `project.config.json`（`urlCheck: false`, `es6: true`, `libVersion: 3.3.4`, `appid: touristappid`）
  - `sitemap.json` 存在
  - **13 个组件**（52 文件）：image-safe, nav-header, modal, bottom-sheet, fullscreen-modal, empty-state, price-tag, user-avatar, rating-stars, tag-chip, companion-fab, product-card, toast
  - **12 个数据模块**：images, banners, library-subcats, market-subcats, brands, social, face-profile, onboarding, library, library-extra, products, local-problems
  - **9 个工具模块**：constants, storage, auth, router, format, system-info, clipboard, scene, recommend
  - **2 个样式文件**：animations.wxss, icons.wxss

- **Phase 3 部分**：5 主 tab 页文件齐全，但仅 2 页为完整实现
  - `pages/home/home` ✅ **完整**（6 区块：面部档案卡 / 闺蜜问候 / 金刚区 / Banner / 热度榜 / 瀑布流 + companion-fab；`onShow` 已用 `this.getTabBar()`）
  - `pages/library/library` ✅ **完整**（Tinder 卡片滑动 movable-view + 瀑布流双模式 + 双数据源 `getLibraryItems` + 多重登录校验）

- **Phase 4-7 部分**：4 个后台 Agent 正在运行（packageA/B/C/D），已产出 6 个完整分包页：
  - packageA: `product-detail` ✅
  - packageB: `template-detail` ✅, `my-templates` ✅
  - packageC: `creator` ✅, `upload` ✅
  - packageD: `search` ✅

### ⚠️ 需修复（核心问题）
- **3 个主 tab 页为 stub 占位**（仅有 `title` + `console.log`，需完整实现）：
  - `pages/mirror/mirror` ❌ —— 需实现 `<camera>` 试妆 + 设置入口 + 步骤拆解教程 + 底部工具栏（滤镜/快门/补光）+ companion-fab
  - `pages/mall/mall` ❌ —— 需实现 搜索框 + 购物车/订单图标(带角标) + 6 分类 tab + 子分类 + 品牌条 + 商品网格(product-card)
  - `pages/profile/profile` ❌ —— 需实现 用户信息卡 + 4 项 stats + 面部档案卡(slim) + GP 大卡 + 3 个功能宫格(9+3+3 项) + companion-fab

### ❌ 未开始
- **Phase 8**：自定义 tabBar —— `custom-tab-bar/` 目录不存在；`app.json` 的 tabBar 未加 `"custom": true`（注意：home.js 已调用 `this.getTabBar().setData({selected:0})`，必须配套自定义 tabBar 否则报错）
- **Phase 9**：编译校验 + 合规检查（需扫描所有页面的 require 路径、usingComponents 引用、HTML 标签残留、DOM 操作等）
- **Phase 10**：Git 提交推送至 `makeuppal-partner` 分支

### 合规检查结果（已通过）
- ✅ 无 `:root` 实际使用（仅 app.wxss 注释中提及，变量挂在 `page` 选择器）
- ✅ 无 ES6 `import/export`（统一用 CommonJS `module.exports`/`require`）
- ✅ 无 `document.`/`window.` DOM 操作
- ✅ 无内联 `<svg>` 在 WXML 中
- ✅ 无 `onclick=`/`innerHTML`/`getElementById`/`querySelector`

## 实施计划

### Phase 3 收尾：实现 3 个 stub 主 tab 页（我自己执行）

#### 3.1 Mirror 焕新页（demo 行 11552-11750）
**文件**：`pages/mirror/mirror.{wxml,wxss,js,json}`（覆盖现有 stub）

**WXML 结构**：
- `mirror-hero-v2`：试妆主区
  - 设置按钮（右上角，emoji ⚙️ 或 CSS 图标，跳转 `mirror-settings`）
  - `mirror-face-area`：人脸引导环 + 标题"AI 虚拟试妆" + 副标题 + 鼓励文案 + 闺蜜引导条（点击 `openCompanionChat`）+ 进度点
  - `<camera>` 组件作为试妆背景（`device-position="front"`，配 `onCameraError` 降级为图片占位）
  - 底部工具栏：滤镜按钮 + 快门按钮 + 补光按钮
- `tutorial-list`：妆容步骤拆解
  - section-header + 进度徽章（"3 / 10 步"）
  - 进度条
  - 面部落笔动线指南（用 `image-safe` 显示面部图 + 文字步骤卡片，**简化 demo 中的 SVG 面部网格覆盖层**为步骤列表）
- `companion-fab` 组件

**JS 逻辑**：
- `data`: cameraReady, currentStep(3), totalSteps(10), stepList(10 项), filterOn, lightOn
- `onLoad`: 初始化步骤列表（从 demo 数据硬编码 10 步：底妆→遮瑕→眉形→眼影→眼线→睫毛→修容→腮红→唇妆→定妆）
- `onCameraError`: 降级 cameraReady=false，显示图片占位
- `onSettingsTap`: `router.navigateTo('mirror-settings')`
- `onShutter`: `wx.showToast({title:'试妆效果已保存', icon:'success'})`
- `onToggleFilter`/`onToggleLight`: 切换状态 + Toast
- `onCompanionTap`: `router.openCompanionChat()`
- `onShow`: `this.getTabBar().setData({selected: 2})`

**WXSS**：试妆主区渐变背景（玫瑰金）、人脸引导环、底部工具栏固定定位、步骤卡片样式、进度条样式

**JSON**：`usingComponents: { image-safe, companion-fab }`, `navigationStyle: custom`（试妆页沉浸式）

#### 3.2 Mall 市集页（demo 行 11337-11417）
**文件**：`pages/mall/mall.{wxml,wxss,js,json}`（覆盖现有 stub）

**WXML 结构**：
- `mall-header`（sticky）：搜索框（`<input>` + 🔍 emoji）+ 右侧订单/购物车图标按钮（带角标 `cartBadge`）
- `mall-tabs`：`<scroll-view scroll-x>` 6 分类（口红/粉底/眼影/腮红/眉笔/护肤，来自 `MARKET_CATS`）
- `mall-sub-tabs`：子分类（来自 `marketSubCats[currentCat]`）
- `brand-strip`：品牌官方区（section-header + 2 张品牌卡：YSL / LANCÔME）
- `product-grid`：商品网格（2 列），用 `product-card` 组件渲染 `marketProducts[currentCat][currentSubCat]`
- `companion-fab` 组件

**JS 逻辑**：
- `data`: currentCat('kouhong'), currentSubCat('全部'), catTabs(MARKET_CATS), subCatList, productList, cartCount
- `onLoad`: 初始化购物车数量（`storage.getCartCount()`）+ 加载默认分类商品
- `onShow`: `this.getTabBar().setData({selected: 3})` + 刷新购物车角标
- `onSearchTap`: `router.openSearch()`
- `onOrdersTap`: `router.openOrders()`
- `onCartTap`: `router.openCart()`
- `onCatTap(e)`: 切换分类，更新 subCatList + productList
- `onSubCatTap(e)`: 切换子分类，过滤 productList
- `onProductTap(e)`: `router.openProductDetail(id)`
- `onBrandTap`: `router.navigateTo('brand')`
- `loadProducts(cat, subCat)`: 从 `marketProducts[cat]` 取数，按 subCat 过滤

**WXSS**：sticky header、搜索框、分类 tab pill 样式、品牌卡、商品网格 2 列

**JSON**：`usingComponents: { image-safe, product-card, companion-fab, empty-state }`

#### 3.3 Profile 我的页（demo 行 11853-12008）
**文件**：`pages/profile/profile.{wxml,wxss,js,json}`（覆盖现有 stub）

**WXML 结构**：
- `profile-banner`：头像（`user-avatar` 组件）+ 姓名"沈婉清" + 等级标签"金妆达人 Lv.5" + bio + 编辑按钮 + 4 项 stats（焕新值 328 / 收藏 86 / 关注 328 / 粉丝 1.2k）
- `profile-face-card`（slim 版）：面部档案卡（image-safe 头像 + 名称 + 骨相分 + 面部属性 + 标签 + 箭头），点击 `openFaceProfile`
- `profile-gp-card`：GP 大卡（⭐ 图标 + 标题 + 副标题 + 余额 328 + "去查看 →"），点击 `openGpCenter`
- `profile-grid-5`：5 项功能宫格（我的模板 💄 / 我的报告 📊 / 工具箱 🧰 / 成长日记 🔔 / 自定义妆容 🎨）
- `profile-grid-3`：3 项（美妆闺蜜设置 🌸 / 创作者中心 ✨ / 邀请好友 🎁）
- `profile-grid-3-bottom`：3 项（门店地图 📍 / 消息中心 🔔 / 设置 ⚙️）
- `companion-fab` 组件

**JS 逻辑**：
- `data`: userInfo({name:'沈婉清', level:'金妆达人 · Lv.5', bio}), gpBalance(328), favCount(86), faceProfile(从 faceProfileData)
- `onLoad`: 从 `getApp().globalData` 同步 gpBalance + 从 storage 读收藏数
- `onShow`: `this.getTabBar().setData({selected: 4})` + 刷新数据
- `onEditProfile`: Toast "编辑资料功能开发中"
- `onFaceProfileTap`: `router.openFaceProfile()`
- `onGpCenterTap`: `router.openGpCenter()`
- 功能宫格点击：分别 `router.openMyTemplates`/`openMyReports`/`openToolbox`/`openDiary`/`openCustomMakeup`/`openCreatorCenter`/`openStoreMap`/`openMessage`/`openInvite` 等
- `onCompanionSettings`: 弹出小美设置（Toast "小美设置开发中" 或跳转）
- `onShareAppMessage`: 分享卡片

**WXSS**：profile-banner 渐变背景、stats 布局、面部档案 slim 卡（横向单行，max-height 80px）、GP 大卡、功能宫格 5 列/3 列网格

**JSON**：`usingComponents: { image-safe, user-avatar, companion-fab }`

### Phase 4-7 验证：分包页面（等待后台 Agent）

后台 Agent 正在创建 32 个分包页。**不重复 spawn**。Agent 完成后执行验证：

1. **文件完整性检查**：每个页面需有 4 文件（wxml/wxss/js/json）
2. **抽查实现质量**：随机读取 5-8 个页面的 JS/WXML，确认非 stub
3. **缺失页补全**：若 Agent 漏掉页面，手动补全

预期 38 页清单：
- packageA (8): product-detail ✅, cart, orders, order-detail, address, address-edit, coupon, favorites
- packageB (9): template-detail ✅, my-templates ✅, my-reports, report-detail, face-profile, diary, custom-makeup, skin-protection, toolbox
- packageC (10): creator ✅, upload ✅, data-center, earnings, academy, brand, style-pref, store-map, message, invite
- packageD (11): search ✅, scan, gp-center, trending, recharge, withdraw, companion-chat, mirror-settings, face-auth, onboarding, share

### Phase 8：自定义 tabBar

#### 8.1 创建 `custom-tab-bar/` 目录（4 文件）
- `index.json`：`{ "component": true }`
- `index.wxml`：5 个 tab 项
  - tab 0 首页 🏠 / tab 1 颜库 💄 / **tab 2 焕新（中间凸起圆角矩形，14px 圆角，凸出 16px）** / tab 3 市集 🛍️ / tab 4 我的 👤
  - 中间焕新按钮用 `<view class="tab-center-btn">` 单独样式（圆角矩形而非圆形，符合用户偏好）
- `index.wxss`：固定底部、5 列 flex、中间按钮凸起 + 玫瑰金渐变 + 阴影、普通 tab 选中态颜色 `#C4956A` / 未选中 `#9B9590`、高度 `var(--tab-bar-height)`
- `index.js`：`Component({ data: { selected: 0, list: TAB_BAR }, methods: { switchTab(e) { wx.switchTab({url: ...}) } } })`

#### 8.2 修改 `app.json`
- tabBar 配置加 `"custom": true`
- 保留 `list`（小程序要求 list 仍需存在作为 fallback）
- color/selectedColor/backgroundColor 保留

#### 8.3 各 tab 页 onShow 联动
- home/profile 已有 `this.getTabBar().setData({selected: N})`
- 补充 library/mirror/mall 的 onShow 联动（library=1, mirror=2, mall=3）

### Phase 9：编译校验 + 合规检查

#### 9.1 全量扫描（用 Grep）
- 扫描所有 `.wxml`：确认无 `<div>/<span>/<img>/<input type=text>/<button onclick>` 等 HTML 标签（应改为 `<view>/<text>/<image>/<input>/<button bindtap>`）
- 扫描所有 `.js`：确认无 `import...from` / `export default` / `document.` / `window.` / `localStorage`（应改 `wx.getStorage`/`wx.setStorage`）
- 扫描所有 `.wxss`：确认无 `:root`（应在 `page` 上）+ 无 `@media (hover)` 等不支持的特性
- 扫描所有 `.json`：确认 usingComponents 路径正确（相对路径 `../../components/xxx/xxx`）

#### 9.2 require 路径校验
- 主包页面：`../../utils/xxx` / `../../data/xxx` / `../../components/xxx/xxx`
- 分包页面：`../../../utils/xxx` / `../../../data/xxx` / `../../../components/xxx/xxx`
- 检查所有 `require()` 引用的文件是否存在

#### 9.3 组件引用校验
- 每个页面的 `.json` 中 usingComponents 引用的组件路径是否正确
- 组件文件是否齐全（4 文件 × 13 组件 = 52 文件）

#### 9.4 数据接口一致性
- 验证 `findTemplateById` / `findProductById` 在所有调用页可正确 import
- 验证 `getImageUrl` 处理 http URL / assets/ 路径 / image key 三种情况
- 验证 storage 工具封装 `wx.getStorageSync`/`wx.setStorageSync`

#### 9.5 修复发现的问题
- 对扫描出的不合规项逐一修复

### Phase 10：Git 提交推送

1. `git status` 查看变更
2. `git add makeuppal-miniprogram/`
3. 提交信息（遵循 `.trae/rules/git-commit-message.md`）：
   - 类型：`feat`
   - 范围：`miniprogram`
   - 描述：完整微信小程序原生转换（43 页 + 自定义 tabBar）
4. 推送至 `makeuppal-partner` 分支

## 假设与决策

1. **不重复 spawn 后台 Agent**：4 个 Agent 仍在运行，等其完成后再验证分包页，不并行 spawn 重复任务
2. **3 个 stub 主 tab 页由我自己实现**：主 tab 页是核心里程碑，质量要求高，不委派 Agent
3. **mirror 页简化 SVG 面部网格**：demo 中的复杂 SVG 面部落笔动线覆盖层在小程序中改为"步骤卡片列表 + 面部参考图"，保留功能逻辑（10 步进度）但简化视觉
4. **camera 组件降级**：开发者工具/无权限时用图片占位，保证页面可预览
5. **custom tabBar 用圆角矩形中间按钮**：遵循用户偏好（圆角矩形而非圆形，减少遮挡）
6. **localStorage 已在 storage.js 封装为 wx.storage**：之前 Phase 0-2 已处理，无需再改
7. **图片域名 `trae-api-cn.mchost.guru`**：project.config.json 已设 `urlCheck: false`，开发者工具可正常加载

## 验证步骤

### 自动验证（AI 执行）
1. Phase 3 完成后：读取 3 个新页面的 JS/WXML，确认非 stub 且结构完整
2. Phase 8 完成后：确认 `custom-tab-bar/` 4 文件齐全 + app.json 有 `"custom": true`
3. Phase 9 全量 Grep 扫描：0 项 HTML 标签残留 / 0 项 ES6 import / 0 项 DOM 操作 / 0 项 :root
4. `require()` 路径与文件存在性交叉校验

### 手动验证（用户在微信开发者工具执行）
1. 导入项目 `makeuppal-miniprogram/` 目录
2. 编译预览，检查 5 个 tab 页切换正常
3. 验证中间"焕新"按钮凸起样式
4. 逐页点击交互：首页金刚区/颜库卡片滑动/焕新试妆/市集商品/我的功能宫格
5. 验证分包页面跳转（商品详情/模板详情/购物车/搜索 等）
6. 检查图片加载（trae-api-cn 域名）
7. 检查 Toast 反馈与登录引导

## 执行顺序

1. **Phase 3.1** → 实现 mirror 页（覆盖 stub）
2. **Phase 3.2** → 实现 mall 页（覆盖 stub）
3. **Phase 3.3** → 实现 profile 页（覆盖 stub）
4. **Phase 8.1** → 创建 custom-tab-bar 4 文件
5. **Phase 8.2** → 修改 app.json 加 `"custom": true`
6. **Phase 8.3** → 补充 library/mirror/mall 的 onShow getTabBar 联动
7. **Phase 4-7 验证** → 检查后台 Agent 产出的分包页完整性，补全缺失页
8. **Phase 9** → 全量合规扫描 + 修复
9. **Phase 10** → Git 提交推送
